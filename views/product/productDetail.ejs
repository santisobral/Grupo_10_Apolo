<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Apolo</title>
    <link rel="stylesheet" href="/css/normalize.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <script src="https://kit.fontawesome.com/98eaad93ae.js" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/css/style-productDetail.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" />
  </head>
<body>
  <%- include("../partial/header.ejs") %> <!--Aca estoy reutilizando codigo gracias a ejs-->
  <main>
    <section>
        <div class="section-main">
          <div class="conteiner-img">
            <div class="imagen-producto">
              <img class="img-grande-producto" src="/img/<%= product.image %>">
            </div>

              <div class="box-img-chica-producto">
                <img class="img-chica-producto" src="/img/<%= product.image %>">
                <img class="img-chica-producto" src="/img/<%= product.image %>">
                <img class="img-chica-producto" src="/img/<%= product.image %>">
              </div>
          </div>
     
          <div class="box-detalle">
            <div class="box-nombre-producto">
              <h1 class="nombre-producto"><%= product.name %></h1>
            </div>
            <div class="box-precio">
              <p class="precio">$ <%= product.price %></p>
            </div>
            <div class="box-tarjeta">
              <p class="tarjeta">Calcula las cuotas con tu medio de pago<i class="far fa-credit-card"></i> <p class="tarjeta"></p>
              </p>
            </div>
            <div class="detalle-producto">
              <div class="descripcion-box"> 
                <h4><%= product.description %></h4>
              </div>
              <div class="talle-lista">
                <label class="box-lista" class="talle2"><b>Talle</b></label>
                <p class="lista control">
                    <select name="size_id">
                     <option value="" disabled selected>Elegir Talle</option>
                     <% sizes.forEach(size => { %>
                     <option value="<%= size.id %>"><%= size.size %> </option> <!--El id de genres es lo que se guarda cuando se envia el form de crear pelicula y es el dato que 
                    necesitas que se guarde dado que esta relacionado con la tabla generos-->
                     <% }) %>
                    </select>
                </p>
              </div>
           
              <a href="/product/productCart">
                <button  title="Agregar al carrito" class="action primary tocart"
                  id="product-addtocart-button"><span>Agregar al carrito</span>
                </button>
              </a>
              <% if ( locals.loggedUser && locals.loggedUser.role_id == 1 ) { %>
                <div class="edit-delete">
                    <a href="/product/editProduct/<%= product.id %>" class="botton-form">EDITAR PRODUCTO</a>
                            <form action="/product/delete/<%= product.id %>?_method=DELETE" method="POST" style="display: inline-flex">
                                <button type="submit" class="botton-form">ELIMINAR</button>
                            </form>
                </div>
                <% } %>
            </div>
          </div>
        </div>
    </section>
  </main>
  <%- include("../partial/footer.ejs") %> <!--Aca estoy reutilizando codigo gracias a ejs-->
</body>

</html>
